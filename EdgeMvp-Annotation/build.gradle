apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"

ext.set("Module","EdgeMvp-Annotation")
apply from: rootProject.file('bintray_publish.gradle')